import 'package:flutter/material.dart';

class MyWidget extends StatelessWidget {
  const MyWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

/// You can cant buy thing but can add products to cart
/// You cant add products to favorite or go to favorites screen
/// profile screen will show option to login
///
/// Phone Number
/// you dont add PN in sign up, but in profile
/// and you really need it when checkout
/// so we make it once user user checkout and then we will not need to make it again
/// + it will decrease pressure on sign up
